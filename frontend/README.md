# Frontend

Commands:

- dev: `npm run dev` (port 5173)
- build: `npm run build`
- test: `npm run test`

The app expects `VITE_API_BASE_URL` to point to the backend API (defaults to `http://localhost:4000`).
